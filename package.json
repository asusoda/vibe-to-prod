{
  "name": "gear-tracker",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "sqlite3": "^6.0.1"
  },
  "engines": {
    "node": ">=22.13"
  }
}
